[ CPU ] ( wiki.archlinux.org/title/CPU_frequency_scaling )
# cpu* в /sys/devices/system/cpu/cpu*/

> sudo echo 0-15 | sudo tee /sys/devices/system/cpu/cpu*/power/energy_perf_bias | режиму роботи
  | wiki.archlinux.org/title/CPU_frequency_scaling#Intel_performance_and_energy_bias_hint

> sudo echo 4700000 | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_max_freq | максимальна частота
> sudo echo 400000 | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_min_freq | мінімальна частота
> sudo echo ... | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_setspeed | постійна частота
  | wiki.archlinux.org/title/CPU_frequency_scaling#Scaling_drivers
> echo performance | tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor | запуск частоти


# intel_pstate в /sys/devices/system/cpu/intel_pstate
> sudo echo 1/0 > /sys/devices/system/cpu/intel_pstate/no_turbo | увімкнення/вимкнення turbo boost
> sudo echo 1/0 > /sys/devices/system/cpu/intel_pstate/hwp_dynamic_boost | увімкнення/вимкнення hwp dynamic boost
> sudo echo 0-100 > /sys/devices/system/cpu/intel_pstate/max_perf_pct | максимальний відсоток продуктивності
> sudo echo 0-100 > /sys/devices/system/cpu/intel_pstate/min_perf_pct | мінімальний відсоток продуктивності

> sudo echo active > /sys/devices/system/cpu/intel_pstate/num_pstates | ?

[ autostart-wm ] автосторат віконого менеджера
> v ~/.bash_profile { startx }
> sudo vim /etc/systemd/system/getty@tty1.service.d/override.conf {
[Service]
ExecStart=
ExecStart=-/usr/bin/agetty --autologin vikarchuk --noclear %I $TERM }

[ power management ] ( wiki.archlinux.org/title/Power_management )
> sudo vim /etc/systemd/logind.conf {
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore }

[ battery-alert ] сповіщення про низький рівень батереї
> потрібний herbe для сповіщення
> v battery-alert {
#!/bin/bash

while true; do
  while [ "$(cat /sys/class/power_supply/BAT0/capacity)" -lt 10 ] && [ "$(cat /sys/class/power_supply/BAT0/status)" = "Discharging" ]; do
    herbe "[ low battery power: ... < 10% ]]]]]]]]]]]]]]]]]]]]]]]"
    sleep 1
  done
  sleep 300
done
}
> bash battery-alert

[ bash ] налаштування вигляду bash
> v ~/.bashrc {
#
# ~/.bashrc
#

# bash
[[ $- != *i* ]] && return

# модефікація команд
alias ls='ls --color=auto -a'
alias v='vim'

# рядок перед курсором
off="\[\033[0m\]"
purple="\[\033[1;35m\]"
green="\[\033[1;32m\]"
red="\[\033[1;31m\]"
export PS1="${green}[${off}${purple}\${PWD/#\$HOME/\~}${off}${green}]${off}${red}\$${off} "

# заговолок вікна
PROMPT_COMMAND='printf "\033]0;${PWD/#$HOME/"~"}\007"'
}
